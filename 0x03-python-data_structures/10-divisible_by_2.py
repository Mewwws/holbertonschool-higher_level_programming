#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    check = []
    for i in my_list:
        if not i % 2:
            check.append(true)
        else:
            check.append(false)
    return check
